name: photo-assets-fetch-batch-03

on:
  pull_request:
    branches:
      - main
    paths:
      - "assets/photo-download-batch-03.json"
      - "assets/photo-download-batch-04.json"
      - "assets/photo-extract-batch-05.json"

permissions:
  contents: write
  pull-requests: read

jobs:
  fetch-photo-sources:
    if: github.event.pull_request.head.repo.full_name == github.repository
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.head_ref }}
          fetch-depth: 0
      - name: Install image validators
        run: python3 -m pip install --quiet pillow pymupdf
      - name: Select photo batch config
        id: batch
        shell: bash
        run: |
          changed="$(git diff --name-only "${{ github.event.pull_request.base.sha }}" "${{ github.event.pull_request.head.sha }}")"
          if printf '%s\n' "$changed" | grep -qx "assets/photo-extract-batch-05.json"; then
            echo "config=assets/photo-extract-batch-05.json" >> "$GITHUB_OUTPUT"
            echo "mode=pdf" >> "$GITHUB_OUTPUT"
          elif printf '%s\n' "$changed" | grep -qx "assets/photo-download-batch-04.json"; then
            echo "config=assets/photo-download-batch-04.json" >> "$GITHUB_OUTPUT"
            echo "mode=image" >> "$GITHUB_OUTPUT"
          else
            echo "config=assets/photo-download-batch-03.json" >> "$GITHUB_OUTPUT"
            echo "mode=image" >> "$GITHUB_OUTPUT"
          fi
      - name: Fetch or extract and validate selected source candidates
        shell: bash
        run: |
          if [ "${{ steps.batch.outputs.mode }}" = "pdf" ]; then
            python3 scripts/extract_pdf_photo_asset.py "${{ steps.batch.outputs.config }}"
          else
            python3 scripts/fetch_photo_assets.py "${{ steps.batch.outputs.config }}"
          fi
      - name: Commit verified source candidates
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add assets/photos/a-core/source-candidates assets/photo-source-registry.json
          if git diff --cached --quiet; then
            echo "No downloaded-source changes."
          else
            git commit -m "Fetch high-resolution replacement photo sources batch 03"
            git push origin HEAD:${{ github.head_ref }}
          fi
