name: photo-assets-fetch

on:
  push:
    branches:
      - publishing/photo-assets-batch-01
    paths:
      - "assets/photo-download-batch-01.json"

permissions:
  contents: write

jobs:
  fetch-photo-sources:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: publishing/photo-assets-batch-01
          fetch-depth: 0
      - name: Install image validator
        run: python3 -m pip install --quiet pillow
      - name: Fetch and validate selected source candidates
        run: python3 scripts/fetch_photo_assets.py assets/photo-download-batch-01.json
      - name: Commit verified source candidates
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add assets/photos/a-core/source-candidates assets/photo-source-registry.json
          if git diff --cached --quiet; then
            echo "No downloaded-source changes."
          else
            git commit -m "Fetch verified A_CORE photo source candidates"
            git push origin HEAD:publishing/photo-assets-batch-01
          fi
