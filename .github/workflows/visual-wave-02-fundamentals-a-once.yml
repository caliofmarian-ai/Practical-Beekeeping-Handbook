name: visual-wave-02-fundamentals-a-once

on:
  push:
    branches:
      - publishing/visual-wave-02-fundamentals-a
    paths:
      - "scripts/generate_wave02_fundamentals_a.py"
      - ".github/workflows/visual-wave-02-fundamentals-a-once.yml"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  generate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: publishing/visual-wave-02-fundamentals-a
          fetch-depth: 0
      - name: Generate Fundamentals A vector assets
        run: python3 scripts/generate_wave02_fundamentals_a.py
      - name: Validate SVGs
        run: |
          python3 - <<'PY'
          import json, xml.etree.ElementTree as ET
          from pathlib import Path
          meta=json.loads(Path("assets/production-briefs/wave-02-fundamentals-a.json").read_text())
          assert meta["asset_count"] == 15
          for a in meta["assets"]:
              p=Path(a["path"]); s=p.read_text(encoding="utf-8")
              ET.fromstring(s)
              assert "<title" in s and "<desc" in s
          print("Validated 15 Wave 02 Fundamentals A assets")
          PY
      - name: Commit generated assets
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add assets/diagrams/wave-02 assets/production-briefs/wave-02-fundamentals-a.json docs/reviews/VISUAL_WAVE_02_FUNDAMENTALS_A_REVIEW.md
          if git diff --cached --quiet; then
            echo "No generated changes"
          else
            git commit -m "Create Wave 02 Fundamentals A visual assets"
            git push origin HEAD:publishing/visual-wave-02-fundamentals-a
          fi
