name: one-time-language-review

on:
  push:
    branches:
      - 'review/language-pass-1'

permissions:
  contents: write

jobs:
  smoke-test:
    runs-on: ubuntu-latest
    steps:
      - name: Confirm runner
        run: echo "language review runner ready"
